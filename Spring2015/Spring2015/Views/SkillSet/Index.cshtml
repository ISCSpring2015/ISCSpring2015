@model IEnumerable<Spring2015.Models.SkillSet>

@{
    
}
<h2> SkillSet</h2>

<script type="text/javascript">
    $(document).ready(function () {
        $("input[type=radio]").click(function () {
            var $this = $(this);
            $this.parents("group:first").find("input[type=radio]").not($this).prop("checked", false);
            $(":hidden[name='hdn_selected_SkillSet']").val() = $('input[name=RdSkillset]:checked').val();
        });
        $(":radio").click(function () {
            var inputValue = $this.val();
            $(":hidden[name='hdn_selected_SkillSet']").val() = inputValue;
        });
    });
</script>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr><th>
</th>
         
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShortName)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Curriculum.Name)
            </th>*@
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>

            <td>
                <input type="radio" name="RdSkillset" id="@item.SkillsetID" value=@item.SkillsetID />
            </td>
   <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShortName)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Curriculum.Name)
            </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SkillsetID }) |
                @Html.ActionLink("Details", "Details", new { id=item.SkillsetID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SkillsetID })
            </td>
        </tr>
    }
    
    </table>
@Html.Hidden("hdn_selected_SkillSet", 0)

<div style="text-align: center;">
   @* @Html.ActionLink("Go To Skills", "Skills", new { id = 1 }, new { @class = "btn btn-primary", @type = "button" })*@
</div>

