@model IEnumerable<Spring2015.Models.Curriculum>


<script>
    var selectedIndex = 0;
    function getSelectedRow(e) {
        alert(e);
        $("#hdnselectedIndex").val() = e;
        selectedIndex = e;
        alert($("#hdnselectedIndex").val());    
    }

    function getValue()
    {
        // alert();
        // var skillcurr = $("form:radio:checked").val();
        var radioanswer = 0;
        if ($('input[name=group1]:checked').val() != null) {
            radioanswer = $('input[name=group1]:checked').val();
        }
        //var selectedId = $('#hdnSelectedId').val();
        // alert(radioanswer);
        if (radioanswer>0) {
        window.location.href = "/SkillSet/Index/" + radioanswer;}
        }
</script>


@{
}

<h2> Curriculum</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Shortname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InitialDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReleaseDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CloseDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Purpose)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <input type="radio" id="radiobtn" name="group1" value="@item.CurriculumID" />
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shortname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InitialDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CloseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Purpose)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CurriculumID }) |
                @Html.ActionLink("Details", "Details", new { id = item.CurriculumID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CurriculumID })
            </td>
        </tr>
    }
    @Html.Hidden("hdnselectedIndex",0,null)
</table>
<div style="text-align: center;">
  
    <input id="btnSkillSet" type="button" name="btnSkillSet" value="Go To Skill Set" class="btn btn-primary" onclick="getValue();" />
   
   
</div>
<script type="text/javascript">
   
</script>